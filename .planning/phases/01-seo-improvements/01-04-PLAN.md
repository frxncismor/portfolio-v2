---
phase: 01-seo-improvements
plan: 04
type: execute
wave: 2
depends_on: [01-01]
files_modified:
  - src/app/pages/services/services.ts
  - src/app/pages/services/services.html
  - src/app/pages/services/services.css
  - src/app/app.routes.ts
  - src/assets/i18n/en.json
  - src/assets/i18n/es.json
autonomous: true
requirements: [REQ-010, REQ-011]
must_haves:
  truths:
    - "Services page is accessible at /services route"
    - "Services page renders real content with service offerings, past clients, and CTA"
    - "Services page content is bilingual (en/es) using the i18n system"
    - "Services page emits ProfessionalService schema markup"
    - "Services page targets SEO keywords: Angular developer Houston TX, freelance frontend consultant, hire Angular developer"
  artifacts:
    - path: "src/app/pages/services/services.ts"
      provides: "Services component with SEO, i18n, and ProfessionalService schema"
      contains: "ProfessionalService"
    - path: "src/app/pages/services/services.html"
      provides: "Services page template with content sections"
      min_lines: 50
    - path: "src/app/app.routes.ts"
      provides: "Route definition for /services"
      contains: "services"
    - path: "src/assets/i18n/en.json"
      provides: "English translation keys for services page"
    - path: "src/assets/i18n/es.json"
      provides: "Spanish translation keys for services page"
  key_links:
    - from: "src/app/app.routes.ts"
      to: "src/app/pages/services/services.ts"
      via: "lazy loaded route"
      pattern: "path.*services.*loadComponent"
    - from: "src/app/pages/services/services.ts"
      to: "src/app/services/seo.service.ts"
      via: "addStructuredData + updateSEO"
      pattern: "seoService\\.addStructuredData"
    - from: "src/app/pages/services/services.html"
      to: "src/assets/i18n/en.json"
      via: "TranslatePipe"
      pattern: "translate\\(\\)"
---

<objective>
Build the services page with SEO-targeted content, bilingual support, ProfessionalService schema, and route registration.

Purpose: The services page is currently empty. Building it out with keyword-targeted content helps rank for "Angular developer Houston TX", "freelance frontend consultant", and "hire Angular developer" — key revenue-driving search queries.

Output: Fully functional services page at /services with content, i18n, schema markup, and route.
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seo-improvements/01-CONTEXT.md
@.planning/phases/01-seo-improvements/01-RESEARCH.md

<interfaces>
<!-- Current empty services component -->
From src/app/pages/services/services.ts:
```typescript
import { Component } from '@angular/core';
@Component({
  selector: 'app-services',
  imports: [],
  templateUrl: './services.html',
  styleUrl: './services.css',
})
export class Services {}
```

<!-- SEOService API -->
From src/app/services/seo.service.ts:
```typescript
export class SEOService {
  updateSEO(data: SEOData): void;
  updateCanonical(path: string): void;
  addStructuredData(data: object, type?: string): void;
}
```

<!-- I18nService API used by other pages -->
From src/app/services/i18n.service.ts (inferred from usage in certifications.ts, price-quote-generation.ts):
```typescript
export class I18nService {
  getLocale(): string;  // returns 'en' or 'es'
  t: Signal<(key: string) => string>;  // translation function signal
  getTranslationValue(key: string): any;
}
```

<!-- TranslatePipe usage pattern (from certifications.ts) -->
```typescript
import { TranslatePipe } from '@pipes/translate.pipe';
// In template: {{ 'services.hero.title' | translate }}
// Or via signal: {{ translate()('services.hero.title') }}
```

<!-- Route pattern from app.routes.ts -->
```typescript
export const routes: Routes = [
  { path: '', redirectTo: 'home', pathMatch: 'full' },
  { path: 'home', loadComponent: () => import('./pages/home/home').then((m) => m.Home) },
  { path: 'certifications', loadComponent: () => import('./pages/certifications/certifications').then((m) => m.Certifications) },
  // ... no services route exists yet
];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Add services route and i18n translation keys</name>
  <files>src/app/app.routes.ts, src/assets/i18n/en.json, src/assets/i18n/es.json</files>
  <read_first>
    - src/app/app.routes.ts
    - src/assets/i18n/en.json
    - src/assets/i18n/es.json
    - src/app/pages/certifications/certifications.ts (reference for i18n pattern)
    - src/app/pages/price-quote-generation/price-quote-generation.ts (reference for i18n pattern)
    - .planning/phases/01-seo-improvements/01-CONTEXT.md (Services Page Content section)
  </read_first>
  <action>
    **src/app/app.routes.ts** — Add the services route. Insert it after the price-quote-generator route (before the blog route), following the existing lazy-load pattern:

    ```typescript
    {
      path: 'services',
      loadComponent: () => import('./pages/services/services').then((m) => m.Services),
    },
    ```

    **src/assets/i18n/en.json** — Add a `"services"` key at the top level with these nested keys:

    ```json
    "services": {
      "seo": {
        "title": "Freelance Angular & React Developer | Houston TX | Francisco Moreno",
        "description": "Hire an experienced Angular developer in Houston, TX. Freelance frontend consultant specializing in Angular, React, TypeScript for enterprise web applications.",
        "keywords": "Angular developer Houston TX, freelance frontend consultant, hire Angular developer, React developer Texas, TypeScript consulting, frontend architecture"
      },
      "hero": {
        "title": "Freelance Web Development Services",
        "subtitle": "Senior Frontend Engineer available for Angular, React, and TypeScript projects",
        "cta": "Get a Free Quote"
      },
      "offerings": {
        "title": "What I Offer",
        "angular": {
          "title": "Angular Development",
          "description": "Enterprise-grade Angular applications with signals, standalone components, RxJS, and NgRx state management."
        },
        "react": {
          "title": "React & Next.js Development",
          "description": "Modern React applications with hooks, server components, and Next.js for SEO-optimized web apps."
        },
        "typescript": {
          "title": "TypeScript Consulting",
          "description": "Type-safe codebases, migration from JavaScript to TypeScript, strict typing strategies for large teams."
        },
        "fullstack": {
          "title": "Full-Stack Development",
          "description": "End-to-end solutions with Node.js, Supabase, AWS, MongoDB, and PostgreSQL backends."
        },
        "uiux": {
          "title": "UI/UX Implementation",
          "description": "Pixel-perfect implementation of designs using TailwindCSS, responsive layouts, and accessibility best practices."
        }
      },
      "clients": {
        "title": "Enterprise Experience",
        "description": "Trusted by teams at leading companies to deliver production-grade web applications.",
        "royalCaribbean": "Royal Caribbean — Cruise booking and management platforms",
        "unitedAirlines": "United Airlines — Flight operations and customer-facing tools",
        "dicksSportingGoods": "Dick's Sporting Goods — E-commerce and retail web applications"
      },
      "process": {
        "title": "How I Work",
        "step1": {
          "title": "Discovery",
          "description": "We discuss your project requirements, timeline, and budget to find the right approach."
        },
        "step2": {
          "title": "Build",
          "description": "I develop your application with regular check-ins, clean code, and thorough testing."
        },
        "step3": {
          "title": "Deliver",
          "description": "Your project is deployed, documented, and ready for production with ongoing support options."
        }
      },
      "cta": {
        "title": "Ready to Start Your Project?",
        "description": "Get an instant estimate with the price quote generator, or schedule a free consultation.",
        "quoteButton": "Get Price Quote",
        "calendlyButton": "Book a Call"
      }
    }
    ```

    **src/assets/i18n/es.json** — Add the corresponding `"services"` key with Spanish translations:

    ```json
    "services": {
      "seo": {
        "title": "Desarrollador Angular y React Freelance | Houston TX | Francisco Moreno",
        "description": "Contrata un desarrollador Angular experimentado en Houston, TX. Consultor frontend freelance especializado en Angular, React, TypeScript para aplicaciones web empresariales.",
        "keywords": "desarrollador Angular Houston TX, consultor frontend freelance, contratar desarrollador Angular, desarrollador React Texas, consultoria TypeScript"
      },
      "hero": {
        "title": "Servicios de Desarrollo Web Freelance",
        "subtitle": "Ingeniero Frontend Senior disponible para proyectos en Angular, React y TypeScript",
        "cta": "Obtener Cotizacion Gratis"
      },
      "offerings": {
        "title": "Lo Que Ofrezco",
        "angular": {
          "title": "Desarrollo Angular",
          "description": "Aplicaciones Angular de nivel empresarial con signals, componentes standalone, RxJS y gestion de estado con NgRx."
        },
        "react": {
          "title": "Desarrollo React y Next.js",
          "description": "Aplicaciones React modernas con hooks, server components y Next.js para apps web optimizadas para SEO."
        },
        "typescript": {
          "title": "Consultoria TypeScript",
          "description": "Codebases type-safe, migracion de JavaScript a TypeScript, estrategias de tipado estricto para equipos grandes."
        },
        "fullstack": {
          "title": "Desarrollo Full-Stack",
          "description": "Soluciones end-to-end con Node.js, Supabase, AWS, MongoDB y backends PostgreSQL."
        },
        "uiux": {
          "title": "Implementacion UI/UX",
          "description": "Implementacion pixel-perfect de disenos usando TailwindCSS, layouts responsivos y mejores practicas de accesibilidad."
        }
      },
      "clients": {
        "title": "Experiencia Empresarial",
        "description": "Confiado por equipos en empresas lideres para entregar aplicaciones web de nivel produccion.",
        "royalCaribbean": "Royal Caribbean — Plataformas de reserva y gestion de cruceros",
        "unitedAirlines": "United Airlines — Operaciones de vuelo y herramientas para clientes",
        "dicksSportingGoods": "Dick's Sporting Goods — Aplicaciones web de e-commerce y retail"
      },
      "process": {
        "title": "Como Trabajo",
        "step1": {
          "title": "Descubrimiento",
          "description": "Discutimos los requisitos de tu proyecto, timeline y presupuesto para encontrar el enfoque correcto."
        },
        "step2": {
          "title": "Construccion",
          "description": "Desarrollo tu aplicacion con check-ins regulares, codigo limpio y pruebas exhaustivas."
        },
        "step3": {
          "title": "Entrega",
          "description": "Tu proyecto se despliega, documenta y esta listo para produccion con opciones de soporte continuo."
        }
      },
      "cta": {
        "title": "Listo para Iniciar tu Proyecto?",
        "description": "Obtén una estimacion instantanea con el generador de cotizaciones, o agenda una consulta gratis.",
        "quoteButton": "Obtener Cotizacion",
        "calendlyButton": "Agendar Llamada"
      }
    }
    ```
  </action>
  <verify>
    <automated>grep -c "services" src/app/app.routes.ts && grep -c "services" src/assets/i18n/en.json && grep -c "services" src/assets/i18n/es.json</automated>
  </verify>
  <acceptance_criteria>
    - `grep "path: 'services'" src/app/app.routes.ts` returns 1 match
    - `grep "loadComponent.*services/services" src/app/app.routes.ts` returns 1 match
    - `grep "Angular developer Houston TX" src/assets/i18n/en.json` returns 1 match
    - `grep "freelance frontend consultant" src/assets/i18n/en.json` returns 1 match
    - `grep "hire Angular developer" src/assets/i18n/en.json` returns 1 match
    - `grep "Royal Caribbean" src/assets/i18n/en.json` returns at least 1 match in services section
    - `grep "desarrollador Angular" src/assets/i18n/es.json` returns at least 1 match
    - Both en.json and es.json are valid JSON (no syntax errors): `node -e "JSON.parse(require('fs').readFileSync('src/assets/i18n/en.json'))"` exits 0
    - Both en.json and es.json are valid JSON: `node -e "JSON.parse(require('fs').readFileSync('src/assets/i18n/es.json'))"` exits 0
  </acceptance_criteria>
  <done>Services route registered at /services with lazy loading. Both en.json and es.json have complete services translation keys covering hero, offerings, clients, process, and CTA sections.</done>
</task>

<task type="auto">
  <name>Task 2: Build services component with content, SEO, and ProfessionalService schema</name>
  <files>src/app/pages/services/services.ts, src/app/pages/services/services.html, src/app/pages/services/services.css</files>
  <read_first>
    - src/app/pages/services/services.ts
    - src/app/pages/services/services.html
    - src/app/pages/services/services.css
    - src/app/pages/certifications/certifications.ts (reference for SEO + i18n pattern)
    - src/app/pages/certifications/certifications.html (reference for Tailwind styling pattern)
    - src/app/pages/price-quote-generation/price-quote-generation.ts (reference for SEO + schema pattern)
    - src/assets/i18n/en.json (to verify translation key paths)
    - .planning/phases/01-seo-improvements/01-RESEARCH.md (REQ-010 ProfessionalService schema)
    - .planning/phases/01-seo-improvements/01-CONTEXT.md (services page content requirements)
  </read_first>
  <action>
    **src/app/pages/services/services.ts** — Rewrite the component:

    ```typescript
    import { Component, inject, OnInit } from '@angular/core';
    import { CommonModule } from '@angular/common';
    import { RouterLink } from '@angular/router';
    import { I18nService } from '@services/i18n.service';
    import { SEOService } from '@services/seo.service';
    import { TranslatePipe } from '@pipes/translate.pipe';

    @Component({
      selector: 'app-services',
      imports: [CommonModule, RouterLink, TranslatePipe],
      templateUrl: './services.html',
      styleUrl: './services.css',
    })
    export class Services implements OnInit {
      private readonly i18nService = inject(I18nService);
      private readonly seoService = inject(SEOService);
      readonly translate = this.i18nService.t;

      ngOnInit(): void {
        const locale = this.i18nService.getLocale();
        const t = this.translate();

        this.seoService.updateCanonical('/services');

        this.seoService.updateSEO({
          title: t('services.seo.title'),
          description: t('services.seo.description'),
          keywords: t('services.seo.keywords'),
          url: '/services',
          type: 'website',
        });

        this.seoService.addStructuredData({
          '@context': 'https://schema.org',
          '@type': 'ProfessionalService',
          name: 'Francisco Moreno — Angular & React Development Services',
          description: 'Freelance frontend development services specializing in Angular, React, and TypeScript. Based in The Woodlands, TX. Available for US clients.',
          url: 'https://frxncismor.dev/services',
          areaServed: [
            { '@type': 'Country', name: 'United States' },
            { '@type': 'AdministrativeArea', name: 'Texas' },
          ],
          provider: {
            '@type': 'Person',
            '@id': 'https://frxncismor.dev/#person',
            name: 'Francisco Moreno',
          },
          serviceType: ['Angular Development', 'React Development', 'TypeScript Consulting', 'Frontend Architecture'],
          hasOfferCatalog: {
            '@type': 'OfferCatalog',
            name: 'Web Development Services',
          },
        }, 'service');
      }
    }
    ```

    **src/app/pages/services/services.html** — Build the full template using translation keys. Follow the Tailwind patterns used by other pages in the project (check certifications.html for reference). The template must include:

    1. **Hero section** — h1 with `services.hero.title`, subtitle, CTA button linking to `/price-quote-generator`
    2. **Offerings grid** — 5 service cards (angular, react, typescript, fullstack, uiux) in a responsive grid (1 col mobile, 2 cols md, 3 cols lg). Each card: icon area, title, description.
    3. **Clients section** — h2 "Enterprise Experience", description, 3 client entries (Royal Caribbean, United Airlines, Dick's Sporting Goods)
    4. **Process section** — h2 "How I Work", 3 steps in a row (Discovery, Build, Deliver) with step numbers
    5. **CTA section** — h2, description, two buttons: one linking to `/price-quote-generator`, one external link to Calendly (use `https://calendly.com/frxncismor` as placeholder)

    All text content MUST use the translate pipe: `{{ translate()('services.hero.title') }}`

    Use semantic HTML: `<section>`, `<h1>`, `<h2>`, `<article>` for service cards. Add the SEO target keywords naturally in the content structure (h1 contains "Web Development", section headings contain "Angular", "React", etc.).

    **src/app/pages/services/services.css** — Add minimal CSS for the services page. Use TailwindCSS utility classes in the HTML template as the primary styling approach (matching the project convention). The CSS file should only contain styles that can't be expressed with Tailwind utilities (e.g., custom animations, specific overrides). Keep it minimal — most pages in this project have near-empty CSS files.
  </action>
  <verify>
    <automated>grep -c "ProfessionalService" src/app/pages/services/services.ts && grep -c "addStructuredData" src/app/pages/services/services.ts && wc -l < src/app/pages/services/services.html && grep -c "translate()" src/app/pages/services/services.html</automated>
  </verify>
  <acceptance_criteria>
    - `grep "ProfessionalService" src/app/pages/services/services.ts` returns at least 1 match
    - `grep "addStructuredData" src/app/pages/services/services.ts` returns 1 match
    - `grep "updateCanonical" src/app/pages/services/services.ts` returns 1 match
    - `grep "updateSEO" src/app/pages/services/services.ts` returns 1 match
    - `grep "areaServed" src/app/pages/services/services.ts` returns 1 match
    - `grep "serviceType" src/app/pages/services/services.ts` returns 1 match
    - `wc -l < src/app/pages/services/services.html` is at least 50 lines (real content, not placeholder)
    - `grep "translate()" src/app/pages/services/services.html` returns at least 10 matches (bilingual content)
    - `grep "routerLink" src/app/pages/services/services.html` returns at least 1 match (CTA links to price-quote-generator)
    - `grep "price-quote-generator" src/app/pages/services/services.html` returns at least 1 match
    - Template contains `<h1>` tag (for SEO)
    - Template contains at least 2 `<section>` tags (semantic HTML)
    - `grep "RouterLink" src/app/pages/services/services.ts` confirms RouterLink is imported
  </acceptance_criteria>
  <done>Services page has full content template with hero, offerings grid, clients, process, and CTA sections. All text uses translate pipe for bilingual support. ProfessionalService schema is emitted. SEO metadata is set. Page links to price-quote-generator via routerLink.</done>
</task>

</tasks>

<verification>
- Navigate to /services in browser — page loads with content (not empty)
- Language toggle switches all services page text between English and Spanish
- `grep -r "ProfessionalService" src/app/pages/services/` returns matches
- `npx ng build` completes without errors
- `grep "path: 'services'" src/app/app.routes.ts` confirms route exists
</verification>

<success_criteria>
REQ-010: Services page emits ProfessionalService schema with areaServed, provider, serviceType, hasOfferCatalog
REQ-011: Services page has real SEO-targeted content with keywords "Angular developer Houston TX", "freelance frontend consultant", "hire Angular developer" — bilingual, with past clients section and CTA to price-quote-generator
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-improvements/01-04-SUMMARY.md`
</output>
