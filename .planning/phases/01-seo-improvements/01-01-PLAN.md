---
phase: 01-seo-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - public/robots.txt
  - public/sitemap.xml
  - public/llms.txt
  - src/index.html
  - src/app/services/seo.service.ts
autonomous: true
requirements: [REQ-002, REQ-003, REQ-004, REQ-005, REQ-006]
must_haves:
  truths:
    - "robots.txt serves correct content with sitemap reference and no extraneous comments"
    - "sitemap.xml uses https://frxncismor.dev/ (not /home) as root URL and includes /services"
    - "llms.txt matches the spec from SITE-STRUCTURE.md"
    - "index.html canonical, og:url, and twitter:url all use https://frxncismor.dev/ (no www, no /home)"
    - "seo.service.ts updateCanonical uses baseUrl property (no www)"
  artifacts:
    - path: "public/robots.txt"
      provides: "Crawler directives with sitemap reference"
      contains: "Sitemap: https://frxncismor.dev/sitemap.xml"
    - path: "public/sitemap.xml"
      provides: "XML sitemap with all public routes"
      contains: "https://frxncismor.dev/"
    - path: "public/llms.txt"
      provides: "AI crawler entity description"
      contains: "Senior Web UI Engineer"
    - path: "src/index.html"
      provides: "Corrected canonical and OG URLs"
    - path: "src/app/services/seo.service.ts"
      provides: "Fixed updateCanonical using baseUrl"
  key_links:
    - from: "public/robots.txt"
      to: "public/sitemap.xml"
      via: "Sitemap directive URL"
      pattern: "Sitemap: https://frxncismor.dev/sitemap.xml"
    - from: "src/app/services/seo.service.ts"
      to: "src/index.html"
      via: "canonical URL base domain match"
      pattern: "frxncismor\\.dev"
---

<objective>
Fix static SEO files and URL inconsistencies across the portfolio site.

Purpose: Ensure search engines can discover all pages via robots.txt/sitemap.xml, AI crawlers get structured info via llms.txt, and all canonical/OG URLs consistently use `https://frxncismor.dev/` (no www, no /home).

Output: Updated robots.txt, sitemap.xml, llms.txt, index.html, seo.service.ts
</objective>

<execution_context>
@$HOME/.claude/get-shit-done/workflows/execute-plan.md
@$HOME/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/01-seo-improvements/01-CONTEXT.md
@.planning/phases/01-seo-improvements/01-RESEARCH.md

<interfaces>
<!-- Current seo.service.ts exports needed for understanding the fix -->
From src/app/services/seo.service.ts:
```typescript
export interface SEOData {
  title?: string;
  description?: string;
  keywords?: string;
  image?: string;
  url?: string;
  type?: string;
  author?: string;
}

@Injectable({ providedIn: 'root' })
export class SEOService {
  private readonly baseUrl = 'https://frxncismor.dev';
  updateSEO(data: SEOData): void;
  addStructuredData(data: object, type?: string): void;
  updateCanonical(path: string): void;  // BUG: line 93 uses hardcoded 'https://www.frxncismor.dev' instead of this.baseUrl
}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Update static files (robots.txt, sitemap.xml, llms.txt)</name>
  <files>public/robots.txt, public/sitemap.xml, public/llms.txt</files>
  <read_first>
    - public/robots.txt
    - public/sitemap.xml
    - public/llms.txt
    - .planning/phases/01-seo-improvements/01-CONTEXT.md (specifics section for exact content)
    - .planning/phases/01-seo-improvements/01-RESEARCH.md (static files section)
  </read_first>
  <action>
    These files already exist but have incorrect/incomplete content. Replace each file entirely.

    **public/robots.txt** — Replace with exactly:
    ```
    User-agent: *
    Allow: /

    Sitemap: https://frxncismor.dev/sitemap.xml
    ```
    No extra comments, no blank lines at end. Per REQ-002.

    **public/sitemap.xml** — Replace with exactly (per REQ-003):
    ```xml
    <?xml version="1.0" encoding="UTF-8"?>
    <urlset xmlns="http://www.sitemaps.org/schemas/sitemap/0.9">
      <url>
        <loc>https://frxncismor.dev/</loc>
        <priority>1.0</priority>
        <changefreq>monthly</changefreq>
      </url>
      <url>
        <loc>https://frxncismor.dev/blog</loc>
        <priority>0.9</priority>
        <changefreq>weekly</changefreq>
      </url>
      <url>
        <loc>https://frxncismor.dev/services</loc>
        <priority>0.8</priority>
        <changefreq>monthly</changefreq>
      </url>
      <url>
        <loc>https://frxncismor.dev/certifications</loc>
        <priority>0.6</priority>
        <changefreq>monthly</changefreq>
      </url>
      <url>
        <loc>https://frxncismor.dev/price-quote-generator</loc>
        <priority>0.5</priority>
        <changefreq>monthly</changefreq>
      </url>
    </urlset>
    ```
    Key changes from current: `/home` replaced with `/`, added `/services`, removed individual blog post URLs (dynamic), added priority and changefreq.

    **public/llms.txt** — Replace with exactly (per REQ-004, from CONTEXT.md specifics):
    ```
    # Francisco Moreno — Senior Web UI Engineer

    Francisco Moreno is a Senior Web UI Engineer based in The Woodlands, TX with 6+ years
    building enterprise web applications for Royal Caribbean, United Airlines, and Dick's Sporting Goods.

    Specializes in Angular, React, TypeScript, RxJS, NgRx, Next.js, Node.js, and AWS.

    Portfolio: https://frxncismor.dev
    Blog: https://frxncismor.dev/blog
    Services: https://frxncismor.dev/services
    LinkedIn: https://linkedin.com/in/frxncismor
    GitHub: https://github.com/frxncismor
    ```
  </action>
  <verify>
    <automated>grep -c "Sitemap: https://frxncismor.dev/sitemap.xml" public/robots.txt && grep -c "https://frxncismor.dev/" public/sitemap.xml && grep -c "/home" public/sitemap.xml | grep "0" && grep -c "Senior Web UI Engineer" public/llms.txt</automated>
  </verify>
  <acceptance_criteria>
    - `grep "Sitemap: https://frxncismor.dev/sitemap.xml" public/robots.txt` returns exactly 1 match
    - `grep "/home" public/sitemap.xml` returns 0 matches
    - `grep "https://frxncismor.dev/" public/sitemap.xml` returns at least 1 match (root URL)
    - `grep "https://frxncismor.dev/services" public/sitemap.xml` returns 1 match
    - `grep "Senior Web UI Engineer" public/llms.txt` returns 1 match
    - `grep "Disallow" public/robots.txt` returns 0 matches (no extra comment lines)
    - `wc -l < public/robots.txt` is 4 (including trailing newline)
  </acceptance_criteria>
  <done>robots.txt has clean directives with sitemap link; sitemap.xml uses / not /home and includes /services; llms.txt matches CONTEXT.md spec</done>
</task>

<task type="auto">
  <name>Task 2: Fix canonical and OG URLs in index.html and seo.service.ts</name>
  <files>src/index.html, src/app/services/seo.service.ts</files>
  <read_first>
    - src/index.html
    - src/app/services/seo.service.ts
    - .planning/phases/01-seo-improvements/01-RESEARCH.md (Canonical & OG URL Fix section)
  </read_first>
  <action>
    **src/index.html** — Per REQ-005 and REQ-006, fix three meta tags:

    Line 22: Change `content="https://www.frxncismor.dev/home"` to `content="https://frxncismor.dev/"`
    (og:url meta tag)

    Line 38: Change `content="https://www.frxncismor.dev/home"` to `content="https://frxncismor.dev/"`
    (twitter:url meta tag)

    Line 49: Change `href="https://www.frxncismor.dev/home"` to `href="https://frxncismor.dev/"`
    (canonical link)

    Also fix og:image on line 28: Change `content="https://www.frxncismor.dev/myphoto.webp"` to `content="https://frxncismor.dev/myphoto.webp"` (remove www)

    Also fix twitter:image on line 44: Change `content="https://www.frxncismor.dev/myphoto.webp"` to `content="https://frxncismor.dev/myphoto.webp"` (remove www)

    **src/app/services/seo.service.ts** — Fix `updateCanonical` method (line 93):

    Change:
    ```typescript
    const url = `https://www.frxncismor.dev${path}`;
    ```
    To:
    ```typescript
    const url = `${this.baseUrl}${path}`;
    ```
    This reuses the existing `private readonly baseUrl = 'https://frxncismor.dev'` on line 23, which is already correct (no www).
  </action>
  <verify>
    <automated>grep -n "www.frxncismor" src/index.html; grep -n "www.frxncismor" src/app/services/seo.service.ts; grep -n "/home" src/index.html | grep -v "loadComponent" | grep -v "redirectTo"</automated>
  </verify>
  <acceptance_criteria>
    - `grep -c "www.frxncismor" src/index.html` returns 0 (no www references remain)
    - `grep -c "www.frxncismor" src/app/services/seo.service.ts` returns 0 (no www references remain)
    - `grep 'rel="canonical"' src/index.html` contains `href="https://frxncismor.dev/"`
    - `grep 'og:url' src/index.html` contains `content="https://frxncismor.dev/"`
    - `grep 'twitter:url' src/index.html` contains `content="https://frxncismor.dev/"`
    - `grep "this.baseUrl" src/app/services/seo.service.ts` appears in the updateCanonical method
  </acceptance_criteria>
  <done>All URLs in index.html use https://frxncismor.dev/ (no www, no /home). seo.service.ts updateCanonical uses this.baseUrl instead of hardcoded www URL.</done>
</task>

</tasks>

<verification>
- `grep -r "www.frxncismor" src/` returns 0 matches — no www references remain anywhere in src/
- `grep "/home" public/sitemap.xml` returns 0 — sitemap doesn't reference /home
- All three static files exist and are non-empty: `wc -l public/robots.txt public/sitemap.xml public/llms.txt`
</verification>

<success_criteria>
REQ-002: robots.txt serves correct Allow + Sitemap directive
REQ-003: sitemap.xml covers /, /blog, /services, /certifications, /price-quote-generator with priorities
REQ-004: llms.txt has Francisco Moreno entity description matching CONTEXT.md spec
REQ-005: canonical URL is https://frxncismor.dev/ (no www, no /home)
REQ-006: og:url is https://frxncismor.dev/ (no www, no /home)
</success_criteria>

<output>
After completion, create `.planning/phases/01-seo-improvements/01-01-SUMMARY.md`
</output>
